[project]
name = "sprite-forge"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["httpx>=0.28"]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
