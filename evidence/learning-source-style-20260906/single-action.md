# 学習開始を一回の操作に統一

## 最終的な配置の訂正

前の配置修正も元の表示と違っていた。`git show e0dc49a:web/intent.js` の実物で、元の開始ボタンは `commentEditor` の「画像の読み取りを確認・編集」の後、「処理の記録」の前にあり、枠内の横幅全体へ伸びることを確認した。学習前の保存済み結果はこの元の表示部品で描画し、同じ位置に一回で開始する操作を挿入する。学習中・完了後の説明保存は維持する。

プレビュー上の案内は「学習が完了するとプレビューへ進めます。」に訂正。実ブラウザーで、開始操作の直前・直後の要素と、ボタン幅565px＝親幅565px、独立したプレビューボタンと案内を確認した。修正前コードは `aefea85fa4f0db065cd7314db07373d83007ec5b`、戻すための画像は `sprite-forge-rollback:before-exact-position-20260906`。以下の位置修正記録は失敗を含む履歴であり、現行配置の根拠にはしない。

2026-09-06。オーナー裁定により、希望の採用案があるだけでは承認を要求しない。未回答の質問がある時だけ止める。

## 修正と確認

- `backend/services.py`：`start_learning` は質問がなければ画像説明・採用方針を保存し、教材準備と学習まで連続実行する。素材の優先指定・参考のみの指定も同じ入口から採用する。
- `web/learning.js`：主操作を「この内容で学習を始める」に統一。質問のない既存の確認待ちにも開始操作を一つだけ表示し、保存済みの説明を残す。ページを開くだけでは学習を起動しない。
- `web/jobs.js`：読み取り中は、終わると学習へ進むことを明示する。
- `AGENTS.md`：合意した一回の操作と説明の保持を反映。
- Python 全275件、WebUI 全44件成功。Python は依存ライブラリの非推奨警告1件。
- 実ブラウザーで、開始一回→読み取り→学習の進捗→完了、説明の保持を確認。質問あり→回答追記→読み取り直し→追加承認なしで完了も確認。幅390pxで横はみ出しなし、教材説明の詳細を展開できる。

画面試験はローカルの一時台帳と試験用AI・GPUを使い、製品の学習サービスとWebUIを接続した。本番素材を学習し直す試験や画質評価は行っていない。学習器自体の変更はない。

## 開始ボタンの位置を修正

同日の追加指示で、開始操作を説明の上から右下の工程操作欄へ移した。`web/flows.js` が操作欄を `web/learning.js` に渡し、学習前・実行中は学習ボタンだけ、完了後は「次へ：プレビュー」を表示する。再学習は既存の折りたたみへ残す。説明や学習処理は変更しない。

実ブラウザーでは製品の制作フロー全体を使い、本文内に開始ボタンがなく、右下のボタンで開始できること、実行中に無効になり、完了後は同じ欄に「次へ：プレビュー」が表示されることを確認。幅390pxで横はみ出しなし。位置修正前のコードは `030affef889da7de441d6d4d6dbac5e347225259`、配備前の画像は `sprite-forge-rollback:before-learning-footer-20260906` に保持する。

## 反映と戻し方

### 元の説明枠内への訂正

右下への移動はオーナーの意図と違っていたため撤回。開始ボタンは最新のAI説明枠の最後の要素とし、枠の外の「次へ：プレビュー」は学習前から表示して完了まで無効にする。開始操作の内容やAI説明は変更しない。実ブラウザーのDOMで説明枠内・末尾であることと、枠外のプレビューボタンが別にあることを確認した。WebUI44件とPython275件成功。変更前のコードは `959edf33fa960b2e8ed2d9e7bd773e1e65d88662`、画像は `sprite-forge-rollback:before-original-position-20260906` に保持する。

通常push後のmainを本番へ反映する。元のコードは `e0dc49ac647898790da36139c1143e1202db1eed`、元のコンテナ画像は `sprite-forge-rollback:before-single-action-20260906` に保存する。戻す場合は元のコードと画像でサービスを再起動する。台帳・素材・説明は移行も削除もしない。
