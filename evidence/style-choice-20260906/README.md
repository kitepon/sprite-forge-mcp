# 画風選択の文言と操作確認

2026-09-06。画風の任意選択と、希望を今回反映しない選択の意味を明示した。

- 選択欄を「他の画風を使いたい場合はこちらから選択」へ変更。
- 「保留」を「今回は画風の希望を反映しない」へ変更。現在の画風設定を維持すること、希望は履歴に残るが自動反映しないことを表示。
- 未選択のまま採用してエラーになる状態を、採用ボタンの無効化と近くの説明で示す。選択は自動で補わない。
- 学習工程では、この選択が学習後の生成用であり、教材を変えないこと、指定画像の画風だけを優先する学習は未対応であることを表示。

## 試験

`tests/web-intent.test.mjs` 13件、`tests/test_intent.py` 16件、`tests/test_style_intent.py` 20件が成功。
全体確認は Python 266件、WebUI 41件が成功。既存の非推奨警告が1件。
最初の全体確認ではエラーメッセージ変更により既存試験の文字列照合が1件失敗し、期待文言を更新して当該試験、全体確認の順に成功した。

`preview.html` は実際の `web/intent.js` を表示する確認用ページ。解釈記録と保存応答だけを模擬する。
リポジトリ直下で `uv run python -m http.server 8774 --bind 127.0.0.1` を起動し、`/evidence/style-choice-20260906/preview.html` を開く。

ブラウザーで390×844と通常幅を確認した。

- 未選択時は採用ボタンが無効。
- 反映しない選択で有効になり、別の画風を選ぶとチェックが解除される。
- 水彩の選択を確認結果へ渡す。
- 設定画では画風設定を維持して衣装の希望だけ採用し、採用済み表示と通知が一致する。
- スマホ幅で横はみ出しなし。

確認用ページには当初通知の表示先がなく採用後の通知で例外が出たため、製品と同じ `#notice` を追加し再確認した。
画像解析・GPU学習・画風の生成品質は今回の試験に含まない。画像別の画風優先学習を実装した証拠ではない。
