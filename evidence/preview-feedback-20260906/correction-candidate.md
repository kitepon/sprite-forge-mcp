# 指摘に基づく修正候補の実測

2026-09-06。ユーザーの17:40:13の「右はサンプル画像と一致してない。髪型が全然違う」という指摘を、元プレビューの候補2に対応させた。元画像のSHA256は `360bb58f80f499c553c72366d7147c2ec0a0be1a66feb64b1ccd2db26e7ff003`。

JoyAIの既存ワークフローへ元画像と元の参考設定画を入力し、髪型だけを参考画像に合わせる指示で生成した。seed 1、30 steps、CFG 4、832×1216。生成72.3秒、初回の範囲抽出まで77.8秒。モデル名・指示全文・生成グラフは同じ閲覧面の処理記録に保持した。VRAMは未計測。

最初のSAM指定 `hair and star hair ornaments` は髪飾りだけを抽出し、合成で髪が元に戻った。目視で不成立を確認した。合成前のモデル出力にはツインテールが描かれていたため、画像は再生成せず、`hair` で元画像と修正画像を再抽出した。双方の和集合を使い、輪郭に残った元画像の画素を含めるため3px拡張した。旧結果は隔離領域の `attempts/` に退避し、失敗を上書き消去していない。

最終候補のツインテール、桃色の毛先、星飾りを目視確認。マスク外で変化したRGB画素数は0。顔・衣装などの保持を利用者が見比べられる状態であり、本人の採否や学習効果を証明する値ではない。

閲覧面: <http://192.168.1.2:8766/api/file?path=local-feedback-candidate-20260906/report.html> 。元画像・候補・マスク・参考画像・合成前出力を掲載し、実ブラウザーで表示を確認した。画像・処理記録はmain-serverの `.cache/local-feedback-candidate-20260906/` に保持する。

候補への本人承認は未記録、LoRA学習は未実行。本番サービス、元の教材、キャラクター台帳、LoRAは変更していない。工程の状態はLatticeに記録する。

## 本人の評価による更新

上記は合成候補を提示した時点の記録。後続会話で、本人は「合成前のモデル出力」を正式な修正候補として進めるよう指示し、「正しく修正できている」と評価した。承認対象は `raw.png` の生成画像そのもの。`candidate.png` の合成画像は不採用とし、教材に含めない。

`approved-pair.json` に元NG画像と承認された生成画像のパス・SHA256、本人の評価原文を記録する。比較画面では承認された生成画像を主表示へ移し、合成画像とそのマスク・測定値は過去の実験として区別する。この承認はLoRA学習の実行記録ではない。
