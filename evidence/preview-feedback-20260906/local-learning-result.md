# 承認された生成画像による局所LoRA学習

2026-09-06。工程正本はLattice `preview-feedback-20260906` の学習実証。元NG画像と、本人が承認したJoyAI生成画像 `raw.png` を使用。合成した `candidate.png` は教材に含めていない。

## 実施条件

既存Anima BaseキャラクターLoRAを凍結基準とし、DiTのLoRAだけを更新した。テキストエンコーダーのLoRA重みは保持。学習サイズ416×608、seed37、20更新、学習率1e-5、beta1、強度0.8。元と修正後に共通の髪マスクを潜在解像度へarea補間し、マスク重み合計×チャネル数でMSEを正規化。同じ時刻とノイズで基準・更新対象、OK・NGを比較した。マスクは損失計算に使い、教材画像の切り貼りはしていない。

比較条件は学習前、承認OKのみ全画像MSE、局所選好。全条件ともAnima Base、832×1216、28steps、CFG4、同じ内容prompt・negative・強度。初回seed101〜110を見た後も学習条件を変えず、確認seed201〜210を生成した。合計60枚を全量目視した。評価者はベルで、ユーザーの採否と区別する。

## 結果

| 条件 | 初回の結び髪 | 確認用の結び髪 | 学習時間 | 最大割当VRAM |
| --- | --- | --- | --- | --- |
| 学習前 | 8/10 | 8/10 | 対象外 | 対象外 |
| OKのみ通常学習 | 6/10 | 8/10 | 19.57秒 | 約9.14GiB |
| 局所選好学習 | 10/10 | 10/10 | 19.22秒 | 約9.14GiB |

局所学習で短髪だった106・109・203・206が結び髪へ変わった。106は参考より髪が長い。203は脚のポーズ、206は口元が変わり、衣装の細部も変化する。白金の衣装・ファー襟・フリルスカート・ブーツという基本構成は全条件で保持したが、全細部の維持を証明する評価ではない。元からあった二人構図と別カット混在は解消していない。

固定基準の重み不変、保存後の再読込み誤差0を両学習で確認。LoRA重み差の二乗和は通常0.1391417、局所0.1337546。損失低下だけで改善と判定せず、新しい出力画像で比較した。

## 閲覧と未達

- 初回全30枚と評価: <http://192.168.1.2:8766/api/file?path=local-learning-20-20260906/report.html>
- 確認用全30枚と評価: <http://192.168.1.2:8766/api/file?path=local-learning-confirm-20260906/report.html>
- 条件・全step誤差・VRAM・時間・教材承認のハッシュは各画面の処理記録に保持。画像・重みは永続キャッシュとfoxに保管しgitには含めない。

髪の改善方向は二組のseedで再現した。ただし一人・一部位・一教材ペアの実証であり、異なる衣装・体形・人外・別部位での汎用性は未検証。特徴維持の細部は未達。これを製品全体の合格や本番採用とは扱わない。採用中のLoRAと台帳は変更していない。

Grok相談はMacとmain-serverの二つの正規CLIで試したが、どちらも共有hookのsymlinkを理由にread-only sandboxを拒否し、質問送信前に停止した。保護設定は変更せず、独立相談は未回収。

通常学習完了後、サーバーの検証用backendが古く結果回収関数が無いため中断。現在の回収コードを同期し、生成済み10枚と完了済み重みを維持して明示再開した。学習の再実行はしていない。本番コンテナの再配備はしていない。

## 学習量を半分にした比較

同じ元LoRA・教材・学習率・学習seedで10更新を実施し、seed101〜110の全30枚を再生成して目視した。これは既に見たseedでの条件比較で、独立した確認集合ではない。結び髪は学習前8/10、通常6/10、局所9/10。局所106は長い下ろし髪となり、107の瞳が紫から青へ変わった。衣装の基本構成は全枚に残るが、胸元・スカート・104右人物の脚姿勢にも変化がある。

10更新を改善案として採用しない。学習回数の削減だけでは、髪の修正と対象外の特徴維持を両立できなかった。局所損失で重みを更新しても生成結果の他領域は変わることを実測した。保持項はまだ未実装・未検証であり、回数調整を成功扱いにして本実装へ進まない。

全30枚と評価: <http://192.168.1.2:8766/api/file?path=local-learning-10-20260906/report.html>。目視記録は `local-10-review.json`。採用中のLoRA・台帳は変更していない。
