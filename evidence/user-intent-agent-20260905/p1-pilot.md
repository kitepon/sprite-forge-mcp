# 画像とコメントの読解: 初回比較

2026-09-05。これは少数の開発実験であり、モデル採用・製品完成・画像品質の受入ではない。

## 実行条件

ユーザーから無料リセット利用の承認を受けて1回使用し、Codex使用率が99%から0%へ戻ったことを確認した。リセットは残り1回。追加購入、APIキー利用、認証ファイルの複製は行っていない。

Codex CLI 0.150.1の既存ChatGPTログインを使用。`--ignore-user-config`、`--ephemeral`、read-only、明示モデル、medium、ChatGPTログイン指定、OpenAI provider指定、Web検索とshell／multi-agent無効、AGENTS読込み上限0を設定した。APIキーとAPI URLの環境変数を子プロセスから除いた。8応答すべてexit 0で、JSONLに記録された完了itemはagent_messageのみ。ツール実行itemはなかった。CLI自身の読込や内部処理がゼロであることを証明するものではない。

同じ4画像を順に添付し、縮小・切抜き・色変更は行っていない。ベルの4枚目は前回調査の画像とSHA-256が一致した。画像、入力、生応答、ログ、hash、時間をgit除外済みの `private/` に保存した。評価観点の `human_review` と以前の診断結果はモデルへ渡していない。

再実行入口は `run_codex_trial.py`。これは個人環境での比較用で、製品・CIからは呼ばない。各回のモデル、case、run名、4画像を明示する。再試行や代替モデルへの切替はない。baselineのmetadataにはinstruction_revisionがなく、後から追加したv2の2件には値がある。baselineの入力は各回のinput.jsonに残っている。

## 共通の初期指示: 2モデル×3ケース

| ケース | Terra | Luna |
| --- | --- | --- |
| 4枚目の衣装を強く採用 | 17.06秒。sample index 3を選択。英文にtwo-pieceはあるが、短丈と腹部露出がない。永続採用を確認なしで提案 | 19.26秒。sample index 3を選択。英文にcropped top。腹部露出は質問へ回し、英文では明示しない。永続採用も提案 |
| 顔は1枚目、服は4枚目 | 15.72秒。index 0／3を分ける。英文の衣装には上下の分離・腹部露出がない。顔の参照に髪・装飾・表情まで含める | 19.74秒。sample 1／4と書き、表示番号と保存用indexが混ざる。髪・表情・画風の参照まで追加し、継続条件にも昇格 |
| 衣装維持、今回は横向き笑顔 | 14.81秒。今回の変更に限定。ただし顔・髪・画風の参照も4枚目と決める。英文の衣装構造が不足 | 17.00秒。今回の変更を分けるが、4枚目を全特徴の正本と解釈。英文が一般的なファー付き衣装にとどまる |

参照の意味を画像から拾う能力はある。どちらも無関係な条件の追加や省略があり、応答をそのまま台帳更新・生成実行へつなげられる結果ではない。sample番号の混同は、自然文の参照文字列を製品の識別子として使えない理由になる。

## 汎用指示の改訂: 2モデル×1ケース

`instruction-v2.txt` で次を補った。

- 後段は画像を受け取らず、テキストだけで生成すること。
- 対象の形・丈・部品の接続と分離・覆う範囲を観察し、画像間の重要な違いを英文でも保持すること。
- 明示的に採用を頼まれた、画像上で明確な特徴を質問へ回さないこと。
- 未指定の共通条件の参照先を捏造せず、継続範囲が不明なら保存を確認待ちにすること。

この指示にはベル、クロップドトップス、スカート、腹部などの固有の正解を含めていない。初期結果を見て改訂したため、このケースは調整用であり、未知の素材への一般化の証拠にはならない。

| モデル | 時間 | 改訂後の結果 |
| --- | --- | --- |
| Terra | 22.32秒 | 短丈トップス、独立した袖、多段フリルの短いスカート、露出した腹部を英文へ記述。継続採用は質問に分け、顔・髪・画風の参照先は未解決として保持 |
| Luna | 17.92秒 | 分離したトップスとスカート、露出した腹部を英文へ記述。継続採用は質問。ただしsample 4という曖昧な参照表記と、袖の記述に曖昧さが残る |

Terraの英文抜粋: “Leave the midriff exposed.” および “Preserve the clearly separated top, detached sleeves, bare midriff, and layered short skirt structure.”

Lunaの英文にも “separated top and skirt, exposed midriff” が入った。生成器へ実際に渡す英文から対象の見た目が消える問題は、この例では汎用指示で改善した。

## 暫定判断と残り

次の試験はTerraを第一候補とする。今回の応答ではLunaより画像参照の対応が安定し、改訂後の構造記述も具体的だった。各条件1回の試行なので品質・速度の順位を確定しない。

次に試すのは参照先の構造化、画像順変更、削除、今回／継続／パネルの範囲、事実と希望の分離、引用、別素材を含む反復。schemaに合うことと、参照・意味が正しいことを分けて確認する。製品へ台帳保存や新しいID体系は追加していない。

Animaの生成は0回、再学習は0回、本番更新は0回。画像の改善は未検証。P1の比較・選定は未完了で、P3の同seed画像比較へまだ進んでいない。

## Claudeの扱い

以下の保留は初回調査時の状態。ユーザーの残量確認を受け、同日中に実測して解消した。結果は末尾に追記する。

[公式ヘルプの更新](https://support.claude.com/en/articles/15036540-use-the-claude-agent-sdk-with-your-claude-plan)では、6月15日に予定していた非対話利用の別枠化は延期され、現在も契約の利用枠を使うと案内されている。一方、[追加使用量](https://support.claude.com/en/articles/12429409-manage-usage-credits-for-paid-claude-plans)は別途有効化できるため、このアカウントの設定確認が必要。

ブラウザーは未ログインで確認できなかった。CLIの読み取り用起動も、bareと異なるsafe-modeで初期確認が出ており、追加使用量の状態は取れていない。MCPや外部ファイルの許可は選ばず、今回起動した2プロセスを終了した。画像・コメントの推論は行っていない。Claudeを品質最下位とは評価しない。OpenAI内の比較であり、別提供元との比較は未完了。

## 検証

8回ともJSON形式で正常終了。完了itemは8件すべてagent_message。私有画像と生応答のgit除外を確認。製品コードと既存の再現試験はこの回に変更していない。前回の全試験結果はPython 71 passed／既知不具合3 xfailed、JavaScript 7 passed。

## 追記: Claude Maxでの3ケース比較

ユーザーから「Claudeのレートリミットは完全に残っている」との確認と続行指示を受けた。CLI自身のrate_limit_eventでも `status=allowed`、`overageStatus=rejected`、`overageDisabledReason=org_level_disabled`、`isUsingOverage=false` を確認した。5時間・週間の利用率は最初の通知でともに0だった。追加使用量の設定を変更せず、契約ログインのまま3件を実行した。

### 呼出し条件

`run_claude_trial.py` はCodexの記録済みinput.json／schema.jsonと同じ4画像を読む。画像hashと入力JSONの一致を確認した。英文の依頼文もCodex版と同じ。画像は公式のstream-json user message内にPNGのbase64として渡し、加工はしない。[画像入力の公式例](https://code.claude.com/docs/en/agent-sdk/streaming-vs-single-mode)

Claude Code 2.1.259、Max／claude.ai／firstParty。モデル別名sonnetで起動し、実際のassistant応答が `claude-sonnet-5` であることを確認。medium、safe-mode、設定読込なし、MCPなし、ファイル／shellツールなし、非永続セッション、fast mode無効。API・別provider・外部OAuth tokenの環境変数は子プロセスから除いた。初期通知は `apiKeySource=none`、ツールは構造化出力用のStructuredOutputのみ。初回実測は単独、残り2件は別プロセス・別出力先で並行実行した。

CLIのmodelUsageには主応答のSonnet 5に加えてHaiku 4.5の少量利用も記録されていた。CLI内部の補助用途はこの記録から特定していない。主応答をHaikuへ切り替えた証拠はないが、CLI経由の利用を「Sonnetだけの呼出し」とは記述しない。modelUsageのcostUSDは `costBasis=list` の見積値であり、追加課金の証拠として扱わない。契約枠の利用確認は上記rate_limit_eventを根拠にする。

### 結果

| ケース | 秒 | 形式修正の失敗回数 | 内容の確認 |
| --- | --- | --- | --- |
| 4枚目の衣装を強く採用 | 101.36 | 4 | 初期指示からクロップトップ・腹部露出・多層スカートを英文に残す。表示4／index 3を明記。ただし継続採用を確定扱いにし、維持指定済みの髪型まで再確認する |
| 顔は1枚目、服は4枚目 | 98.46 | 3 | 顔index 0／服index 3を分け、英文に腹部露出と独立した袖を含める。ただし既存画風をsample 0〜2のpainterly animeと決め、補助参照の画像や表情まで追加する |
| 衣装維持、今回は横向き笑顔 | 106.75 | 4 | 今回の向き・表情を分ける。別画像のコートを混ぜないよう指定。ただし継続変更欄にも維持条件を書き、英文は画像番号への依存と腹部露出の省略が残る |

3件ともexit 0、result subtype=success、is_error=false、最終のstructured_outputあり。形式修正では、最初にJSON文字列をStructuredOutputキーで包んで渡し、schema不一致になった。最後は正しいルート項目を直接渡して成功した。実験スクリプトは再呼出ししていないが、CLI内部では修正の会話が続いており、1発で形式が合った成功とは区別する。内部の試行を消したり、途中の仮データを結果として採用したりしていない。

### 比較後の判断

次の比較対象をTerraとSonnet 5に絞る。Sonnetは初期指示から対象衣装の違いを英文へ残す点が有望。Terraは改訂指示で同様の内容を残せており、今回のCLI条件では形式修正を繰り返さなかった。モデルそのものの速度差と断定せず、CLIの形式修正を含む利用者の待ち時間として上表を扱う。

Lunaは画像番号とindexの混同が残るため、次の主比較から外す。最終採用はまだ決めない。共通の改善指示、参照先の構造化、未指定の条件の保持を揃えて反復する。Sonnetで改訂指示v2を試すことも未実施。画像生成と本番更新は行っていない。

## その後のユーザー裁定: Sonnetを除外

2026-09-05、「いや。sonnet遅かったんだろ。捨てていいよ。」との指示を受け、上記のTerra／Sonnet追加比較案を撤回した。Sonnetの98.46〜106.75秒はCLIの形式修正を含む実測待ち時間である。Sonnetの再試験と接続方式の改善調査は行わず、Terraの12ケース×3回の品質検証へ進む。既存の実測結果と生応答は保存する。これは候補除外の決定であり、Terraの品質合格や本番採用を意味しない。

追記時の検証: Claude用実験スクリプトの構文確認、3結果の項目型検査、対応するCodex入力との一致を確認。全試験はPython 71 passed／既知不具合3 xfailed／依存の非推奨警告1件、JavaScript 7 passed。空白差分も確認した。再現済みの保存不具合は未修正。
