# 設定画の注文反映を全6枚で確認

2026-09-05、開始33e189a、clean。前単位は人間を基準にした解釈修理と全211件の確認を完了していた。今回も親だけが書き込み、既存の統括計画を引き継いだ。

## 素材と解釈

本番台帳はBellとベル、画風はbell_glow。GPU一覧に男性教材の学習済みキャラクターLoRAはなかった。既存のanima_joy_sprite_loraは赤髪の魔法使いを学習したものだったため代用せず、学習済みのベルを使用した。男性の解釈試験は過去の結果として保持するが、今回を別キャラクターの画像品質の証拠にはしない。

既存4枚の画像で、正面・側面・靴の3項目を構成する実解釈が32.61秒、そこへ「4枚目を基本に、今回だけトップを青、スカートと靴は白、顔と髪と項目の役割は維持」という注文を重ねる実解釈が64.40秒で完了した。正式CLIのgpt-5.6-terra medium、ChatGPT認証で各1回。追加課金・リセット・Sonnetなし。

親が全案と4枚目を目視した。構成は全3項目で4枚目を参照し、注文は白い基本衣装を共通と個別へ保存、正面と側面へ青い今回衣装、靴へ白い継続条件を出した。顔立ち・髪は対象別の今回条件。親がこの案を隔離台帳へ確認・採用し、追加のモデル呼出しは行わず生成した。

## 生成と環境の失敗

MacのPythonからGPUのqueue取得に失敗し、最初の実験は生成前に終了。専用出力には本番台帳の前後JSONだけがあり、両者は一致、GPUキューは空。curlの同じGPUへの接続は成功した。このMac経路の問題は過去の実測にも記録されており、今回は既存の実行手順を先に使わず同じ失敗を再走した。製品の通信処理は変更していない。

メインサーバーの `/tmp/sprite-sheet-images-Piwudv` にコードと入力を置き、旧本番イメージの隔離コンテナで生成した。変更前の3枚ができた後、日本語フォント不足によりシート合成が失敗した。元ジョブ `79d183c8-faeb-493c-9db8-0698682f7920` はfailedのまま保持している。

現行Dockerfileにはfonts-noto-cjkと対応フォントの設定が既にある。本番には未配備だったため、そのDockerfileから実験用 `sprite-forge-sheet-probe:20260905` をビルドした。依存は公式aptとuvの既存手順。比較スクリプトへ明示的な `--baseline-job` を加え、生成済み3枚を再生成せず共通の合成処理だけ回収した。回収の情報はcomparison_recoveryへ記録し、元の失敗ジョブは書き換えていない。

同じ隔離台帳で注文を確認し、変更後の3枚とシートを生成した。新ジョブ `e3f4978d-5804-4ee3-bbdc-7fdb093e9877` はcompleted。両コンテナは終了し、--rmで除去済み。実験用イメージと入出力を保持している。本番サービスは停止・再構築していない。

## 条件と全画像の観察

Anima Base v1.0、ベルの既存LoRA `ndac1de01_01bffcbe.safetensors`、model/clipとも0.8。28 steps、CFG 4、Euler/simple。Seedは正面1、側面3、靴23。変更前後の実グラフ3対は、肯定文と否定文以外の全ノードが完全一致した。衣装記述の詳細と顔立ちの句も変わるため、色の語句だけの一要因比較ではない。

| 項目 | 変更前／後の秒 | 親の目視 |
| --- | --- | --- |
| 正面 | 6.1／6.0 | 白から青いトップへ変化。白いスカート・ブーツ、紫の目・ピンクの毛先は保持。両方とも正面と背面の2体で、1項目1方向にはならない |
| 側面 | 6.0／6.0 | 青い上着が現れるが前面に白が残り、正面用の衣装と形も揃わない。両方とも正面と側面の2体。右向き指定に対して側面像は左向き |
| 靴 | 7.0／7.0 | 白と金のブーツに脚が入る。全身衣装や顔は生成文に入っていないが、否定条件で除外した脚を描く |

本番台帳は前後で一致。生成後の隔離台帳の共通条件に青いトップは残っておらず、白い基本衣装と今回の青は分離されている。元画像に手を加えて修理していない。

## 表示・コード・試験

`scripts/compare_sheet_intent.py` を追加。構成保存・提案確認・設定画生成は製品の共通サービスへ通し、実グラフと台帳・ジョブを記録する。合成失敗時の回収は実験の明示操作だけで、自動再試行や製品の代替経路にはしていない。`scripts/check_intent.py` の既存応答再利用へ工程引数を追加し、従来のpreview既定を保った。

比較準備・対象別参照の再対応と、失敗ジョブを書き換えず合成だけを回収する試験を追加。focused各1件、最終関連12件成功。製品コードとUIは無変更。最終全Python試験は213件成功、6.81秒、既存依存の非推奨警告1件。差分検査も成功した。

生データはローカル `private/bell-sheet-images-server-v1/` とサーバーの上記専用領域へ保存。構成・注文の生応答は `private/bell-sheet-layout-v1/`、`private/bell-sheet-order-v1/`。親の観察は `sheet-image-observations.json`、表示は `render_sheet_images.py` で画像比較へ全観察と全解釈を加える。

[比較画面](http://192.168.1.2:8766/api/file?path=/app/intent-sheet-images-20260905.html) へ全6枚・実条件・失敗と回収・未達内容・2件の全解釈を掲載した。HTTP 200、実ブラウザーで6枚の比較欄と観察文を確認。各画像はローカルでも全て目視した。表示HTMLだけを既存配信へコピーした。

## 判定と次の確認

注文を確認して生成へ届け、今回条件を基本条件へ残さない動作を実画像まで確認した。画像の色は変わったが、設定画の構図と靴単品は未達であり、画像品質の合格にはしない。次は同じ実グラフを基に、構図の句とLoRAの影響を一要因ずつ比較する。原因を確かめる前に製品の語句辞書や再学習を追加しない。

構成を解釈する指示には、今回の案でfaceへ笑み・赤面が入る点も残る。描画注文では静的な顔に置換されたが、構成案の特徴分離は後続の修理対象として計画へ記録する。本番接続と画風の工程も未完了。新たなオーナー裁定を要求する状態ではない。
