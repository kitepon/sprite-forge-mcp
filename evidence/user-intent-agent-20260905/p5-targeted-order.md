# 人間の注文を基準にした対象別案の修理候補

2026-09-05。開始HEAD `da240d1`。親が[設計](../../docs/adr/20260905-targeted-order-design.md)に沿って指示を変更し、同じ人物画像・原文で2件を実測した。候補は人間の受入未達のため不採用とし、製品の指示は元へ戻した。未達を隠して本番へ反映していない。

## 実施と結果

`instruction-targeted-v1.txt` は試した指示の全量。既存schema・適用順・保存処理・生成モデルを変更せず、意味上の対象へ必要な案を出すことと、同一特徴の未変更部分・既存除外を保持することを補強した。キャラクター名・衣装・参照番号ごとの分岐は追加していない。

既存CLI、gpt-5.6-terra medium、ChatGPT認証を使用。新規の隔離cacheで各1回、追加課金・APIキー・リセット・Sonnet・再試行なし。画像と原文は前回と同一。

| 人間の構成 | 秒 | 生成文まで確認した結果 |
| --- | --- | --- |
| 画像から作った正面・側面・靴の3項目 | 40.76 | 正面・側面に対象付きthis_runが届き、マントは緑。保存案は赤、白いズボンの除外と顔・髪・向き・靴を保持。ただし今後の基本衣装を個別panelだけで返し、共通persistentを省略 |
| 従来23項目 | 43.69 | 通常衣装と衣装単品は今回だけ緑、共通保存は赤。別衣装と靴を巻き込まない。一方、faceの `small closed mouth` が笑顔・驚きの `open mouth` と競合 |

前回の従来構成のfaceは `youthful face with a large blue eye, fair skin` で、口を閉じる指定はなかった。各1回の比較なので指示変更だけが混入の原因とは断定しないが、今回の候補で人間の表情維持を満たしたとは言えない。新構成の共通条件不足も残る。基本衣装を個別項目へ届ける案は、他工程へ残す共通条件の代わりにはならない。

目録 `targeted-order-cases.json` に全2応答を載せ、既存の表示スクリプトで画像・原文・全解釈・実際の解決処理による生成文を表示した。[閲覧ページ](http://192.168.1.2:8766/api/file?path=/app/intent-targeted-order-20260905.html) はHTTP 200と実ブラウザーの両ケース・参考画像・生成文の展開欄を確認。表示HTMLだけを既存配信へコピーし、本番機能の変更・再起動はしていない。

## 試験と採否

`tests/test_sheet_panel_intent.py` に、対象付き衣装変更が顔・髪・正面と側面・別衣装・靴・既存除外を保持する試験を追加。初回は置換案のavoidを空にしたまま旧avoidの保持を期待したため失敗した。特徴全体を置換する既存契約に合わせ、完成形の案へ除外を明記して修正した。製品処理を試験に合わせて変更していない。focused 1件成功。

関連試験 `uv run python -m pytest tests/test_sheet_panel_intent.py tests/test_reference_probe.py tests/test_intent_experiment.py` は40件成功、2.15秒。既存依存の非推奨警告1件。`git diff --check` 成功。製品の指示をHEADの内容へapply_patchで戻した後、同ファイルのdiffが空であることを確認した。試した候補は実験資料だけに残す。

人間の受入に未達のためスライム追加試験と実画像生成は実施しない。最終全試験は個別機能の受入が済んでいないため実施しない。学習・本番機能配備・pushなし。既存stash保持。親の判定は「対象別反映の改善を確認したが候補不採用」。次は人間の静的な顔と表情の分離、および共通の継続希望を対象別保存へ縮めない点を、同じ小さい修理の続きとして扱う。全ゴールの完了・オーナー裁定待ちとはしない。
