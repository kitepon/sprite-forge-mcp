# 人数・視点・採用した特徴を構成案へ残す

## 結果

LoRAを0.8のまま維持し、構図に一人・一方向を明示した比較で、正面と側面が一人になった。紫の目、桃色の毛先、白金の衣装、塗りの特徴は残る。右向き指定への左向き、トップの袖や前面に残る白、正面と側面での肩の形など衣装差は未達。構図改善の候補を得た段階で、画像品質全体の合格ではない。

構成エージェントは各枠の人数・視点・範囲、顔と表情の分離、肯定と除外の分離、採用した特徴の具体値を案へ残すよう修理した。英語句の強制追記、キャラクター名・衣装・参照番号に応じたコード分岐は追加していない。指定がある複数方向の比較図も扱い、全枠を単独人物に制限していない。

全11画像（既存8、新規3）と今回の4応答を[閲覧ページ](http://192.168.1.2:8766/api/file?path=/app/intent-sheet-single-view-20260905.html)へ掲載。途中の不採用応答も省略していない。本番設定と台帳は未変更。

## 追加生成3件

前回の変更後3枚の実グラフを使い、実験スクリプト `probe_sheet_causes.py` で指定入力だけを変更した。

| 条件 | seed | 秒 | 観察 |
| --- | --- | --- | --- |
| 側面のcharacter reference除去＋LoRA model/clipとも0 | 3 | 6.0 | 左向きの側面が二人。二要因を同時に変えても人数は解消しない |
| 正面の構図をsolo, one person, full body, single viewへ変更、LoRA 0.8 | 1 | 6.2 | 正面の一人。画風と特徴を保つがトップの一部は白い |
| 側面の構図を同様に変更、LoRA 0.8 | 3 | 6.0 | 側面の一人。右指定に対して左向きで、衣装差も残る |

条件は `sheet-interaction-cases.json` と `sheet-single-view-cases.json`。親が原因を調べるために作った英語句であり、エージェントが生成した文と混同しない。単語の個別効果までは分離していない。構図の表現全体を変えた効果として扱う。

メインサーバー `/tmp/sprite-sheet-images-Piwudv` の既存隔離環境で、`results/interaction1` と `results/singleview1` を新規に作成した。各docker runはexit 0。LoRA無効化は診断だけで、製品には採用しない。再試行・再学習なし。

ローカル回収先は `private/sheet-interaction-v1`、`private/sheet-single-view-v1`。保存済み結果・実グラフ・画像・GPU情報を保持。今回の出力にイベントファイルはない。レンダラーが前回5件と今回3件の実グラフを、宣言した変更後の期待グラフへ全体比較して8件一致。seedと結果の対応、全新規画像が一度ずつ掲載されることも照合した。SHA-256は `private/intent-sheet-single-view-20260905.verification.json` と閲覧ページに保存。

## 実際の構成解釈4件

同じ正式CLI入口、gpt-5.6-terra／medium／ChatGPT認証で実行した。生記録の `interpreter.auth` は4件ともchatgpt。直接API・追加使用量・認証変更は使っていない。既存のローカル正式ログインを使う方式と構造化出力を[公式の非対話実行資料](https://learn.chatgpt.com/docs/non-interactive-mode)で再確認した（2026-09-05）。モデル・呼出し・schema・公開APIは変更していない。

1. `private/bell-sheet-layout-v2`、29.81秒。同じ4枚・同じ原文。正面一人・側面一人・靴一足の3項目、参照4枚目、顔立ちと表情の分離を確認。最初の指示修正での応答であり、後続の修正後に再実行した記録ではない。
2. `private/human-layout-count-v1`、29.93秒。別の男性2枚。「正面と背面を同じ枠、開口笑顔、靴一足」の3項目。数量と表情は正しいが、靴の肯定句にno humansが入り、未達として保持した。
3. `private/human-layout-count-v2`、31.38秒。同じ入力で除外欄の指示を明確化。靴の除外は移ったが、共通設定が空なのに比較図の顔を継承へ任せて青い目を落とした。これも未達として保持した。
4. `private/human-layout-count-v3`、29.60秒。同じ入力で、採用した特徴はpartsへ具体値を残すよう修理。比較図と顔アップに青い目・茶髪を保持、開口笑顔はexpression、靴の除外はavoid_enへ分離。各参照は2枚目、3項目、同一枠の二方向を保持。親が全parts・役割・継承を読み、今回修理した契約として受け入れた。男性の実画像生成はこの比較では行っていない。

失敗した実応答で、肯定句への除外命令の混入と、空の継承元に任せた顔の欠落を確認した。それぞれの置き場所と継承の意味を指示へ補い、同じ入力で上記の変化を確認している。resolverは各partを利用する既存実装を維持している。画像参照と日本語説明があるだけでは、欠けた英語の特徴値は生成へ補われない。

## 検証

変更前の構成解釈13試験が成功。変更ごとに同じ対象を確認し、最後に各枠の数量・空の共通設定でも顔を保持・単品の肯定と否定の分離が既存の生成文へ届く試験を追加。関連59試験成功（2.38秒）、最終Python 216試験成功（6.61秒）、既存依存の非推奨警告1件。WebUI製品コードは未変更なので、その試験の再走はしていない。

レンダラーは複数の実験結果・条件ファイルと全解釈記録を受け取るよう拡張し、画像件数を目録から数える。実入力の照合で8グラフ一致・11画像掲載。閲覧用HTMLだけを本番コンテナへコピーし、HTTP 200、ブラウザーのタブ29で全11画像要素、観察文、4つの実応答欄を確認。サービス再起動・学習・本番台帳の変更はしていない。

## 未解決と次の修理

`backend/bible.py` の共通NEGATIVEに `multiple views, reference sheet, collage` が無条件に含まれる。今回の「同じ枠へ二方向を並べる」という正しい解釈も、生成段階で固定否定と衝突する。構成案を返せたことを、その比較図の生成成功には数えない。

実応答v3の比較図を `panel_from` と `resolve_panel` に通す最小再現で、肯定文に `two full-body character views side by side`、否定文に `multiple views, reference sheet, collage` が同時に出ることをassertで確認した。コード検索だけを根拠にしていない。

次はこの固定否定を、旧来の単独パネルの挙動を保ちながら、確認した構成と注文に従う配置へ直す。人間の単独立ち姿で悪化しない比較も必要。右向き・衣装の細部・靴単品の脚も未完了として残す。P3/P4/P5/P6の残り、サブスクリプション限定、明示学習、人間を主な受入にする方針を変更しない。オーナーの裁定待ちはまだない。
