# 構成案の解釈と確認編集の実測

2026-09-05。開始HEAD `499f7c4`、clean、fetch後0 behind・19 ahead。対象はSprite Forge。既存stashを維持し、親だけが書き込んだ。設計は [構成案の契約](../../docs/adr/20260905-layout-editor-design.md)。本記録は機能全体の完成宣言ではない。

## 実装と独立反証

注文の保存に `layout` 工程を加え、注文時の構成・編集中の構成・参照を記録する。構成専用の出力形式と指示を既存CLIへ渡し、原文と全案を保存する。確認・訂正後に構成保存サービスへ渡す専用のREST/MCPを公開した。構成案を共通の描画条件として誤採用しない。

WebUIの設定画工程に構成一覧を置いた。追加・削除・順序・名前・区分・内容を編集でき、入力途中は下書き、確定結果はサーバーへ残る。原文・構成の未確定中はその画面から生成を開始しない。不採用を記録する操作は案を履歴から消さず、再表示で採用候補へ戻さない。競合した下書きを新しい構成へ無言で重ねない。

既存の読取り担当による最終反証で以下を再現し、親が修理した。

- 同じSeed差分のまま既存内部キーを別名に変えた案が通り、項目別指示を落とす。解釈・確定の2境界で期待する例外が出ない最小試験を確認した。外部の案が既存Seed差分へ新規キーを付けた場合に明示エラーとし、台帳全体が不変なことを試験した。
- 同じ特徴を複数断片に持つ項目で、優先チェックが二つ出る。一つを外すと他方の表示と保存値が食い違う。チェックを特徴ごとに一つにまとめ、特徴を変更しても旧特徴の断片が残る間はその優先指定を残した。最小試験は修理前に `2 != 1` で失敗し、修理後に保存値まで一致した。

この2件以外の未解決P0/P1は独立反証から返っていない。同じ差分の再監査は行わず、親が最小再現と修理後の試験で閉じた。

## 試験

ベースラインは構成14件・CLI10件、計24件成功。最初の存在しない試験名の指定は未実行。実装中のfixture試験は構成原文・案・訂正保存、質問、不正参照、古い構成、空欄、専用CLI形式、REST/MCP、取消を対象にした。

最後の反証再現で `uv run pytest` を使った起動は `backend` のimportに失敗し、製品を実行していない。`uv run python -m pytest` によりrepoをimport可能にして、上記2境界の実際の失敗を確認した。製品コードをimportエラーへの対処として変更していない。

| 確認 | 実行と結果 |
| --- | --- |
| 最後の関連試験 | `uv run python -m pytest tests/test_layout_intent.py tests/test_sheet_layout.py tests/test_intent_cli.py` — 37件成功、1.18秒 |
| 画面の最小試験 | `node --test tests/web-layout.test.mjs` — 7件成功、42.02ms |
| 最終Python | `uv run python -m pytest` — 205件成功、6.96秒 |
| 最終JavaScript | `node --test tests/web-*.test.mjs` — 29件成功、46.43ms |
| 差分衛生 | `git diff --check` 成功 |

ローカルPythonは3.14.7。StarletteのBlockingPortal非推奨警告1件は残る。本番3.13での確認には代用しない。

途中の全JavaScript試験は、直前の個別試験にあった仮想DOMの検査方法の誤りを確認する前に起動し、26/27件となった。仮想DOMのtextContent検査を修正し、個別5件成功後に27件成功を確認した。最初から全試験が成功したとは扱わない。最後の独立反証2件を修理してから、上表の最終確認を行った。

## 実モデルでの構成案

既存の公式Codex CLI、`gpt-5.6-terra`、medium、ChatGPT契約ログインを維持した。APIキー・追加購入・追加使用量・Sonnet・リセットは使っていない。自動再試行はない。資料確認には [Codex execの公式説明](https://learn.chatgpt.com/docs/developer-commands#codex-exec)を参照し、実際の出力形式と認証指定はローカル契約試験と実呼出しで確認した。

| 入力 | 時間 | 観測 |
| --- | --- | --- |
| 成人男性。鎧を青い水着へ変更し、白い下着を追加。側面は保持 | 17.62秒 | 希望の内容と3項目、既存キー・Seed差分を保持。新しい名前が英語となった |
| 手足・衣装のない青いスライム。正面・側面・跳ねる姿の3項目 | 18.53秒 | 3項目へ構成を変更し、各項目の被写体条件に体色と形を保持。手足と服を避ける条件を返した |
| 変更しない旧名と新しい日本語名の区別を指示へ追記して、最初の入力を再測定 | 24.3秒 | 水着と白いボクサーブリーフは日本語名、未変更のSIDEは維持 |

全案は未採用。画像添付のない構成試験なので、画像を読めた証拠にはしない。隔離した生応答は `private/layout-live-outfit/interpretation.json`、`private/layout-live-creature/interpretation.json`、`private/layout-live-outfit-ja/interpretation.json`。元構成も保存し、修正前の応答を含めて [閲覧ページ](http://192.168.1.2:8766/api/file?path=/app/intent-layout-agent-20260905.html)へ載せた。HTTP 200と実ブラウザーの3結果表示を確認した。

## ブラウザー

`tests/intent_demo.py --layout-demo`、専用 `private/layout-ui-20260905`、既存スライム素材を使った隔離画面。解釈とGPUはfixtureである。

- 正面・側面の順序と名称変更、項目追加、詳細の英文、再表示での下書き保持を確認した。
- 未確定のまま生成を押すと最初は通信エラー表示になった。ローカルの構成検査を同期呼出しにして、送信前の入力エラーとして「入力を確認してください／シート構成を確定してから生成してください」と表示した。GPU要求は送らない。
- 詳細欄の配列をDOMへ直接appendして `[object HTMLLabelElement]` が出る原因を直し、実際のチェック欄を確認した。
- 構成案の処理中は原文保存済みと表示し、操作欄を無効化した。架空の進捗率や残り時間は出さない。返された案から不要な項目を外して確定し、2/2パネルの模擬生成完了を確認した。
- 案を保存済み構成へ戻すと注文欄が空になり、再表示後も不採用案は復活しなかった。
- 同じ特徴への変更で優先欄が一つとなり、一方を衣装へ変更すると残った姿勢と衣装が両方選択済みになった。確定・再表示後も二つの保存設定を表示した。確認後、試験用の構成を元の特徴へ戻した。
- 幅390pxで文書幅375px、カードの右端321px、横はみ出しなし。画面も目視した。表示幅の一時変更は解除した。

再表示直後のDOM検査が一度3秒で時間切れとなったが、次の画面取得で保存済み状態と設定の一致を確認した。製品の再試行分岐は追加していない。

## 境界と残作業

衣装名・キャラクター・参照番号の専用分岐は製品に追加していない。今回の衣装とスライムは実験スクリプトの入力例に限る。通常の注文解釈では、基本装備の単品条件を継続保存し、体色を被写体へ分類する指示を追加した。この二つの実モデル修理確認はまだ行っていない。

変更対象は `backend/{app,intent,intent_cli,intent_runner,intent_service,sheet_layout}.py`、二つの解釈指示、`web/{api,flows,jobs,layout}.js`、CSS、構成試験とデモ、実験・閲覧スクリプトと本単位の文書。機能本体の本番配備、実GPU生成、実学習は本単位では未実施。閲覧HTMLの更新で本番アプリを再起動していない。次は参照画像付きの構成案、通常注文の範囲と体色、実画像効果、画風の解決、本番接続を進める。再学習は教材と条件を示した明示合意まで起動しない。
