# 人間の注文を保つ解釈修理の実測

2026-09-05、開始HEAD `6405a99`、clean、追跡済みorigin/mainとの差0 behind・22 ahead。リモートの再取得はしていない。既存stash保持。前単位は候補の不採用と新しい失敗の記録を終えた進捗であり、同じモデル呼出しを再起動していない。

## 変更

親が `backend/intent_instructions.txt` を修理した。対象別注文を必要な項目へ出す既存候補を基に、顔立ちの形とその時の表情を区別し、共通の継続希望を個別保存だけへ縮めない指示を加えた。対象別の完成形では同一特徴の未変更部分・取り消されていない除外を保持する。

生成側の優先順位・schema・保存処理・モデル・UIは変更していない。キャラクター名・衣装・参照番号による分岐、語句の後処理・自動再試行はない。試験3件の間で指示を変更していない。指示のSHA-256は `a9754eb9d148fc91bf8049791efcd4355687f3318cf4b197021603adaa298b24`。

## 実モデルの全3件

正式CLI、gpt-5.6-terra medium、ChatGPT認証。人間は既存のhero-battle・hero-relax、補助試験は既存slime画像。原文・構成は前回と同一。新規の隔離台帳でそれぞれ1回ずつ実行した。APIキー・追加課金・リセット・Sonnetは使用していない。

| ケース | 秒 | 確認した内容 |
| --- | --- | --- |
| 人間・画像付き新構成 | 46.18 | 基本衣装はpersistentと必要なpanelへ保存する案、今回の正面・側面は緑。顔・髪・向きと白いズボンの除外を保持。靴は靴の内容だけ |
| 人間・従来23項目 | 42.29 | faceは青い目などの静的な顔立ちだけ。笑顔・驚きにclosed mouthが混ざらず、向きと別衣装を維持。衣装単品は今回だけ緑、靴は茶色のブーツを継続 |
| 人外・補助3項目 | 59.58 | 人間の生成文を確認した後に同じ指示で実行。全項目に今回だけ緑が届き、共通と項目別に形と目の特徴を保存する案。跳ねる項目に地面で休む姿勢を混ぜない |

全件questionsは空。親が全changesを読み、`render_reference_probe.py` の `resolve_candidate` で現在の生成処理へ渡して文を確認した。実際の採用・GPU生成は実行していない。従来構成の衣装案は画像の腰の鞘も含む。新構成には剣・盾の除外があり、それを維持している。原文と既存条件が違う箇所を同一の指定として扱わない。

目録 `human-order-cases.json` が全3件の生応答と観測を指す。[閲覧ページ](http://192.168.1.2:8766/api/file?path=/app/intent-human-order-20260905.html) に原文・画像・提案・実際の生成文を掲載した。HTTP 200と実ブラウザーで全3件の見出し・参考画像・生成文の展開欄を確認した。HTMLは既存配信へのコピーだけで、機能本体の本番更新・再起動はしていない。

## 試験

`tests/test_sheet_panel_intent.py` に、静的な顔の指定が表情を変えず、対象別の今回衣装と共通・個別の継続衣装を分ける試験を追加。生成側は旧実装のままで成功した。直前の関連40件成功をベースラインとして使用。

初回 `uv run pytest ... -k static_face_and_common_outfit` はbackendのimportに失敗して収集できなかった。既存のmodule起動 `uv run python -m pytest ...` ではfocused 1件が0.06秒で成功。これは起動時のsys.pathの違いで、テスト対象の製品ロジックは変更していない。テストコマンド失敗の回収と新試験の実行は実モデル1件目を実行中に行った。標準試験入口の問題は計画の保守記録へ残した。

関連 `tests/test_sheet_panel_intent.py tests/test_intent.py tests/test_reference_probe.py tests/test_intent_experiment.py` は56件成功、2.11秒。その後に最終全試験 `uv run python -m pytest` を1回実行し、211件成功、6.79秒。既存依存の非推奨警告1件。`git diff --check` 成功。UI無変更なのでWebUI試験を再走していない。

## 親の判定

人間の主受入と補助試験を生成文まで満たしたため、指示修理を採用する。各条件1回の成立をモデルの普遍的な保証にしない。確認・訂正を挟む既存仕様を保つ。人間の成功を確認する前に特殊条件へ進めず、特殊条件のために生成側の優先度を変えていない。

実画像品質、画風指定、本番接続は未完了。実機学習は合意前に起動しない。次は人間の実画像を既存Anima＋LoRAで確認する。現在の単位は修理と解釈確認として閉じ、全ゴールは継続する。
