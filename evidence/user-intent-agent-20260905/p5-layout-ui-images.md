# 人間の構成案をアプリで確定し、実画像まで確認する

## 判定

人間の保存済み実エージェント応答を、現行アプリの確定操作からAnima＋LoRAの3画像・設定画合成・完成表示まで接続できた。正面と左側面は各一人、靴は脚のない一足。上下セパレートの衣装、紫の目、白金色と桃色の髪を確認した。[参考4画像と全3生成画像](http://192.168.1.2:8766/api/file?path=/app/intent-layout-ui-20260905.html)を掲載している。

靴単品には履き口のファーがなく、立ち姿の靴とも細部が異なる。元の解釈案の靴条件にもファーが記されていない。装飾の形・配置や髪の広がりまで完全一致したとは判定しない。人間の画像品質全体、プレビュー・一枚生成の修正後の画像、本番からの新しいCLI呼出し、本番機能配備は未完了。

## 条件と隔離

開始HEADはbbc084e、作業ツリーclean。前単位は既定条件の修理・実解釈・226試験成功までの進捗。今回は製品コードを変更せず、既存計画内の直接検証として親が実行した。

- 保存済み実応答: `private/bell-sheet-layout-v2/interpretation.json`。元の解釈は公式CLI・ChatGPT契約ログインのgpt-5.6-terra、29.81秒。今回再実行していない。
- 原文: 「4枚目の衣装と顔と髪を基に、正面の立ち姿・側面の立ち姿・靴の単品の合計3項目にして。向きは各項目に合わせて。衣装は今の白と金の配色を維持して。」
- 比較元の学習済みキャラクターは本番のベル。台帳をHTTPで読み、呼び出し語・LoRA名を隔離台帳に設定した。旧生成物・本番教材を上書きしていない。
- main-serverの一時ディレクトリ `/tmp/sprite-layout-ui-ymjU0R` へ、HEADのbackend・web・scriptsと今回の準備・回収スクリプト、保存済み応答・参考画像をコピー。旧本番コンテナではなく、日本語フォント入りの既存隔離イメージを使用した。
- 検証アプリはホストの127.0.0.1:18768だけで待受け、Macから一時SSH転送で閲覧した。本番アプリのポート・起動・設定は変更していない。MacからGPUへの既知の接続失敗は再走していない。

## 確定前の準備

`scripts/prepare_layout_ui.py` は元応答と同じ順序の画像を隔離台帳へ追加し、参考画像の参照だけを新しい台帳へ結び直す。元の原文、構成、提案の文章、解釈モデルを保持し、保存済み応答の再利用であることと元ジョブを記録する。確定操作やGPU生成は行わない。

外部の構成案は製品の検証関数を通し、確認待ちで保存。元応答と画像のSHA-256、準備結果は `private/layout-ui-v1/preparation.json` に保持した。元の構成が当時の既定から変わっている場合も、その履歴を現在の既定へ書き換えず、元構成を隔離台帳の比較用に保存する。

新しいfocused試験 `tests/test_prepare_layout_ui.py` は1件成功（0.14秒）。元応答の不変、参照の対応、確認待ちの状態、未確定の構成、LoRAと呼び出し語の保持、GPU未要求を確認した。

## アプリで行った操作

実ブラウザーのタブ32で制作の続きを開き、設定画工程へ移動した。原文、3項目と各参考画像、確認待ちの表示を確認して「この構成を確定」を押した。保存済み表示の後に再読込みし、原文・項目・保存状態が残ることを確認した。生成前の実GPUキューは空だった。

「設定画を作る」を押すと、処理中のボタン・開始応答待ち・経過時間を表示した。完了後は「できました」「3 / 3 パネル」と保存済み設定画が表示された。拡大画面で合成画像を確認した。未確定案をAPIで直接採用したり、生成APIを手で呼んだりして、このUI操作を代用していない。

生成ジョブは `51fe3cd2-dc5b-4c91-a0c2-b3a953618c74`、確定した構成ジョブは `f283fabe-fd73-4ea6-8b03-8876a6c89f8b`。各画像は6.0、6.0、7.0秒。Seedは1、3、23。Anima Base 1.0と既存キャラクターLoRA 0.8、28step、CFG 4を使用した。再学習・モデル変更・自動再試行はない。

## 実行記録と画像の照合

`scripts/collect_layout_ui.py` で、保存済み応答の提案とアプリの確定案が一致し、その構成が生成ジョブに使われたことを検査した。ComfyUI履歴からこのジョブのclient_idだけを抽出し、全3グラフを現在の生成要求と完全一致で照合した。GPUが返した画像へ製品と同じ余白処理を行い、アプリの保存画像とバイト単位で一致することも検査した。本番台帳は検証前後で一致した。

台帳・ジョブ・イベント・3画像・合成画像・生成HTML・原文と元提案・GPU情報・該当GPU履歴・照合JSONを `private/layout-ui-v1` に回収した。他ジョブのGPU履歴は保存していない。閲覧用スクリプトは生成ジョブの全項目と観察文の対応、各画像のSHA-256を検査して表示する。タブ33で参考4画像・生成3画像・全観察文の表示を確認した。

最終Pythonは227件成功（6.98秒）、既存依存の非推奨警告1件。生成物を使った照合・閲覧ページの出力も成功した。追加・変更ファイルは準備と回収のスクリプト、準備の試験、既存閲覧スクリプト、入力目録と本記録・計画。製品のbackendとwebは未変更。

## 終了と残作業

生成終了を確認して一時検証コンテナとSSH転送を停止した。検証データは残している。本番へコピーしたのは閲覧用HTMLのみで、機能配備・サービス再起動・学習は行っていない。

画像拡大時に `null` が出る表示不具合を確認。`web/ui.js` のlightboxが単画像の時に標準DOMのappendへnullを渡すことが原因で、最終保守工程へ記録した。今回の生成接続の実測には製品修理を混ぜていない。

今回の接続成功を本番CLI接続や全画質の合格へ一般化しない。画風変更の扱い、プレビュー・一枚生成の画像確認、靴と衣装細部、本番接続・配備を計画に保持する。全体ゴールは継続し、今の段階で新しいオーナー裁定は必要ない。
