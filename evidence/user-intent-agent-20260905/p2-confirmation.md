# 原文・解釈案・訂正の保存とプレビュー接続

2026-09-05。ローカルの隔離台帳で確認した。本番の台帳・教材・LoRAは未変更。

## 実装と個別試験

- 原文をモデルより先に保存。画像順・画像コメント・当時の設定を同じ解釈ジョブへ固定する。
- 確認した特徴だけを共通条件へ保存。今回だけの姿勢等はジョブに残し、別特徴とLoRA設定は維持する。
- 参照外の画像、削除済み画像、同じ特徴の並行更新、再作成した対象、重複や異なるパネル範囲は明示エラー。
- sample indexは削除後も再利用しない。旧台帳は削除前の最大値から次番号を導出する。
- プレビューの姿勢・構図は特徴ごとに置換し、正面と横向きを連結しない。既定外の自由文と確定条件は自動で混ぜない。
- CLIは正式なChatGPTログイン、ユーザー設定なし、ツール無効。API用環境変数を除外する試験と、ツール実行・未完了応答を拒否する試験を実施した。
- `PYTHONPATH=. uv run pytest tests/test_intent.py tests/test_intent_cli.py -q` は20件成功。その後、CLI環境変数・一時画像の試験を追加し、同ファイル7件成功。
- 保存応答B→Aの逆転、パネルへの保存body、未採用注文と自由入力経路のJavaScript試験3件成功。

## 実ブラウザー

`tests/intent_demo.py`で実HTTPサーバーを起動し、GPUと解釈だけfixtureに置き換えた。`private/p2-browser-01/`〜`03/`が隔離台帳。

- 4枚の画像を表示。4枚目の未保存コメントと全体への注文を入力し、学習工程へ移動後に戻って両方の保存を確認。
- プレビューの解釈案の英語を `cropped top, separate skirt, bare midriff`、避ける内容を日本語「帽子」・英語 `hat` に訂正して採用。再表示後も保持し、2枚のfixture生成が完了。
- 採用後の「原文を保存」でdraftへ戻らず、確認した条件で生成できる。
- 解釈fixtureを失敗させ、原文と失敗理由が再読み込み後も残る。
- リンクで作品一覧へ離脱した直後、保存APIで「画面を離れる直前の注文」がdraftとして残ることを確認。
- 正面パネルに「正面だけ帽子を外す」と入力し、側面へ切り替えた直後にAPIで `stage=panel`・`panel=turn_front` と原文が保存されていることを確認。
- 390×844の画面で参考画像カード・注文欄・操作が重ならず、documentの横幅375に対してviewport390、横はみ出しなし。除外条件を英語の折り畳みだけへ隠さず日本語でも示す。

## 実CLI

`scripts/check_intent.py`でアプリの実入口を3回実行。GPU生成・再学習なし。各原文、提案、モデル、時間、認証は `private/p2-native-01/`〜`03/jobs/` に保存。生応答を選別削除していない。

| 試験 | 秒 | 確認した結果 |
| --- | ---: | --- |
| 1 | 19.28 | 4枚目・衣装・継続を選択。ただし操作指示と顔髪の維持文が生成文へ混入 |
| 2 | 26.71 | 短い英語句へ改善。ただし腹部露出を誤って除外条件へ出力 |
| 3 | 24.56 | 衣装のみ・4枚目・継続。二部式、腹部露出、独立袖、フリル、ブーツを肯定条件へ保持。除外なし |

2件の意味上の誤りを受け、操作指示を生成文に混ぜないこと、除外は明示的な除外注文だけにすることを汎用指示へ加えた。除外の日本語表示も必須にした。固有名・衣装名による分岐はない。最終例の成功は、自動採用や別素材での一般化の証明ではない。

正式なCodex CLIをChatGPTログインで使用。Sonnet、直接API、追加購入、リセット、認証ファイルの転用はない。公式根拠は[非対話実行](https://learn.chatgpt.com/docs/non-interactive-mode)とCLIの実測。

## 独立反証と採否

読み取り担当が実ファイルを確認。生成と解釈のjob ID分離、既存設定保持、ホスト間の画像転送、ツール無効化を採用。プレビューの既定語と確定条件の競合は、特徴別の所有範囲で解消した。

最終レビューの4件（パネル原文欄の欠落、保存応答逆転、未確認draftによる自由入力の阻止、直リンクでの保存迂回）を採用して修理。保存世代と対応する戻り値、パネル別原文欄、自由入力経路、画面離脱前の保存を実装した。

## 最終試験と残る工程

`PYTHONPATH=. uv run pytest -q`: **99件成功**（4.24秒）。依存先Starlette/anyioの非推奨警告1件。`node --test tests/*.test.mjs`: **12件成功**。失敗した試験なし。

学習用の観察説明、画風LoRAへの解釈、設定画・パネル・新しい一枚への生成条件接続、本番コンテナからの正式CLI接続は後続工程。未接続の工程では保存のみと明示し、解釈ボタンを出さない。画風条件の採用も、未接続のまま台帳へ入れて生成を壊さず、理由を表示して止める。本証跡は製品全体の完成・配備を宣言しない。
