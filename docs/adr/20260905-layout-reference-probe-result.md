# 画像付き構成と人間の注文の実測判定

2026-09-05。親の判定として、[実測](../../evidence/user-intent-agent-20260905/p5-reference-probe.md)を検証単位の結果として受け入れる。製品修理や汎用性全体の合格ではない。

画像付きの人間用3項目構成は2枚目を選び、衣装・顔・髪・靴を具体化した。従来構成での靴の継続案も改善した。一方、新構成へ同じ注文を重ねると、今回だけ緑にするマントが赤のまま組み立てられる。これはスライムの体色と同じ対象別案の不足で、人間の基本動作も未達である。

既存の読取り担当の反証を受け、schemaと適用順を保って対象別案の出力を補強する修理候補を採用する。コードへの実装は後続。別衣装と単品への無差別配布、共通this_runを役割条件より上にする変更は採用しない。対象別の完成形には同一featureだけを書き、姿勢や表情を混ぜない。修理の主受入は人間の衣装・別衣装・靴・向きの維持とし、人外だけの成功で代用しない。

関連10件成功、4回の実応答と生成文、閲覧面を確認した。全目標は継続中。修理後の同条件実測、実画像の効果、画風指定、本番機能配備が残る。再学習は明示的な実行合意まで行わない。現時点でオーナー固有の追加裁定を要する停止はない。
