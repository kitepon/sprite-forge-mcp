# 設定画の注文を実画像で確認する

開始点33e189a、clean。前単位は人間を基準にした解釈修理と全211件の最終確認を完了した進捗。orchestrateの既存計画を引き継ぎ、親だけが実験スクリプトと記録を書く。

本番台帳とGPUのLoRA一覧を読み、男性教材に対応する学習済みキャラクターLoRAは見つからなかった。既存のanima_joy_sprite_loraは赤髪の魔法使いの教材由来であり、男性の本人確認には使わない。再学習を起動せず、学習済みのベルで正面・側面・靴の構成と衣装色変更を確認する。別キャラクターの画像品質を合格とはしない。

4枚の既存画像で構成を解釈し、親が全案を確認する。確定した3項目構成へ、4枚目を基本とし今回だけトップを青にする注文を解釈する。親が確認後、隔離台帳で構成のみの3枚と注文を反映した3枚を同じseed・既存Anima＋LoRAで生成する。LoRA・強度・モデル・seed・寸法は固定。正面と側面の向き、顔・髪、衣装の部品と色、靴単品を画像で見る。未変更の靴も、全身衣装が混入しないことを確認するため含める。

製品の共通サービスへ構成保存・提案確認・設定画生成を通す。実験では親が確認した生応答を再利用し、画像・参照を新しい隔離台帳へ対応付ける。正式CLIの追加呼出しは構成1回・注文1回だけ。自動採用の製品仕様、再試行、後処理による衣装修復を加えない。実グラフと全画像、前後の本番台帳を記録し、閲覧面へ掲載する。

本番機能配備・再学習・追加課金・リセット・Sonnet・pushは行わない。GPUが稼働中なら既存処理を止めない。結果待ちの中断時は同じ実行ハンドルを回収して重複実行しない。生成の解釈失敗と画像品質の未達は分ける。
