# NG事例の再発抑制を研究対象に戻す

2026-09-07、オーナーは「特定のNGの事例を生成させないっていう研究」「髪型の研究ではない」と訂正し、実行を指示した。対象は画像と指摘に基づく共通のNG反映方法であり、部位ごとの専用学習器ではない。前の髪全体の注意領域を細分化する案は継続しない。旧試作の失敗記録は維持する。

学習器の入力は、画像の出典、本人の指摘原文、観察根拠、否定された具体的特徴を対応付けて作る。特定の特徴を既定値にせず、肯定の指摘を消去対象にしない。NGの説明がない場合は全画像の否定と推測しない。OK画像やOK/NGペアを必須にしない。指摘に無い特徴の保持は、生成後に別途確認する。

最初の共通方式は、画像と指摘を視覚言語モデルで解釈し、その具体的特徴をESDの入力へ接続するもの。ESDは一般の概念除去法だが、この接続では画像の情報を文章へ要約するため、文章で識別できない形の再発抑制まで保証しない。少数画像から概念表現を学習する方式は、その限界が実測された場合の比較候補とする。

本人の13件を残し、同じ画像への衣装に関する研究用指摘、複数カットに関する研究用指摘、否定対象が不明な研究用指摘を併記して、共通処理がコメントに従って対象を変えるか確認する。研究用指摘は本人の評価ではない。同一人物の複数特徴で成立しても、別の体形・人外を含む製品汎用性の証明とは扱わない。

工程と未完了状態は既存Lattice研究タスクに記録する。本書は裁定を固定し、作業一覧を複製しない。

## 変更裁定：NGを受けた元LoRAの作り直し

2026-09-07、オーナーは「方向性を変える。NG画像とその理由を受けて（ここはいっぱい考えてきたよね表現）、元LoRAを作り直す」と指示した。

以後の研究は、NG画像と指摘の解釈を元教材の採否・説明・構成へ反映し、Anima BaseからLoRAを再学習する方法を対象とする。ESDによる既存LoRAの追加更新は過去の比較実験として保存する。

入力はNG画像、その理由の原文、元教材、元の制作意図を対応させる。これまでの画像観察・指摘対象の解釈を引き継ぐ。NGの一部特徴を画像全体の否定へ拡大せず、指摘のない特徴と既存の制作意図を維持する。元画像・元LoRAは保持し、作り直した教材とLoRAを別版として対応付ける。NG画像の単純追加、未判定の生成画像のOK扱い、特定部位の専用処理は採用しない。

教材の変更内容と理由を画像と一緒に閲覧できる形で残し、その教材を用いて再学習する。新LoRAは元LoRAと同条件で生成比較し、指定されたNGの再発と他の特徴の維持を確認する。
