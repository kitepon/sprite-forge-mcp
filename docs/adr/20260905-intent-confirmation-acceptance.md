# コメントの保存・確認画面の受入

2026-09-05。親がP2の保存・提案・訂正と、P3のプレビュー入口を受け入れる。独立反証で採用した指摘を修理し、個別試験後の最終試験はPython 99件・JavaScript 12件成功した。[実測と反証の記録](../../evidence/user-intent-agent-20260905/p2-confirmation.md)を根拠とする。

原文は画像順・コメント・当時の設定とともにモデル呼出し前に保存する。採用は利用者による明示操作だけ。今回限定の条件は共通台帳に昇格させない。sample indexの再利用修理は既存番号体系の範囲とする。

生成用の英語は、参照番号や操作指示でなく具体的な描画条件にする。除外条件は日本語も表示する。実CLIで意味上の誤りが2例出たため、自動採用を許可する判定はしない。正式CLIのChatGPTログインでTerraを一回ずつ呼び、API課金・Sonnet・認証転用・自動再試行はしない。

プレビューの姿勢と構図の既定値は、それぞれ同じ特徴の確定条件が置換する。自由入力の英語を意味分解する辞書や追加モデル呼出しは置かず、確定条件と既定外の自由入力の同時指定は明示エラーとする。未採用の注文があるだけで自由入力経路を阻止しない。

学習・画風・設定画・パネル・新しい一枚への解釈反映はP4/P5の責務として残す。現在のUIは該当工程を原文保存のみと明示する。画風の未接続条件を採用済みとして永続化しない。本番コンテナからホストCLIへの接続と配備も未受入であり、この単位だけで製品全体の完成とはしない。

次は計画済みのP3、同じ既存LoRA・同じSeed 1/2による新旧4枚比較。実験結果を利用者が見て衣装の方向を決められるところまで進める。再学習はその必要性と実行合意を確認するまで起動しない。
