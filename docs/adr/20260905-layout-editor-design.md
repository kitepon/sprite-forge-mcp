# 構成案を言葉で作り、編集して確定する

2026-09-05。開始HEAD 499f7c4、clean、fetch後0 behind・19 ahead。既存stashを維持する。前回のゴール応答は状態確認で、実装の進捗ではなかった。構成変更の承認を受けて本単位へ進む。

既存の注文保存・解釈ジョブへlayout工程を追加する。構成の原文、画像順、画像コメント、既存構成を保存してから、既存の契約ログインのCLIで構成案を一回返す。案は日本語の説明、参照、描く条件を持ち、質問が残る間は確定しない。確定前は構成も共通条件も画像も変更しない。確定は既存の構成保存サービスを呼び、元の構成との競合を検出する。原文と訂正後の案をジョブへ残す。

設定画工程に構成一覧と編集を置く。項目名・区分・内容を確認し、追加・削除・上下移動ができる。日本語での訂正は注文へ含めて再解釈し、生成へ渡す英語条件は詳細欄で直接編集できる。入力途中の原文と構成案を端末の下書きへ保存し、確定結果はサーバーへ保存する。確認・解釈待ちの構成がある間は、その画面から生成を開始しない。別の工程の注文を構成の注文へ混ぜない。

書込みは親だけが担当し、公開契約の独立反証は既存の読取り担当へ依頼する。UIと保存・解釈契約が結合するため書込みを分割並列化しない。ベースラインは構成14件・CLI10件が成功。最初に存在しない試験名を指定した起動は未実行として扱う。

任意の人型・人外・衣装の固定分岐を設けない。既存モデル・認証・Anima＋LoRA・明示学習を維持する。本単位は構成提案と編集の接続を対象とし、本番配備と実画像評価は後続。focused testで質問・不正参照・古い構成・確定前不変・再表示・編集を確認し、最後に関連全試験とブラウザーで確認する。
