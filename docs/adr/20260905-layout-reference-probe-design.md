# 画像付き構成と通常注文の再確認

2026-09-05。開始HEAD a7918bf、clean、追跡済みorigin/mainとの差は0 behind・20 ahead。前単位は実装・試験・commitを完了した進捗である。本単位は新しい製品契約を設けず、既存契約の実モデル検証を親が直接行う。

既存の男性2画像とスライム1画像を再利用する。男性の通常注文は既存のgeneric-casesと同一にし、基本の靴がscope=panelとして継続するかを確かめる。スライムは前単位で返された3項目構成を隔離台帳へ保存し、同じ体色注文を読み直す。衣装や靴の固定パネルとの衝突を取り除いた条件で、体色の分類を確かめる。画像付きの構成案では男性の2枚目を指定し、正面・側面・靴の3項目を求める。参照の選択、濃灰色のズボン、茶色のブーツ、不要な剣や盾の扱いを画像と照合する。

公式CLI・gpt-5.6-terra medium・ChatGPT契約ログインを維持する。原文・参照順・全応答を各専用cacheへ保存する。比較画像は描画せず、まず解釈の成立を判断する。実生成は解釈を確認した後の別作業とし、再学習・本番台帳への採用・直接APIを行わない。解釈実験の入口へ、確認済み構成を入力する引数とlayout工程を加える。ベースライン6件成功。引数と保存構成・参照の伝達をfocused testで確認してから実モデルを呼ぶ。

3実測は個別に確認できるが同じ契約利用枠を消費するため、親が一件ずつ実行して応答を確認する。子への書込み委譲は不要。失敗を除外せず、元画像と全案・所要時間・判定を閲覧面へ出す。新しい公開API・保存契約を変える必要が見つかった場合は、この実測だけで無断実装せず原因と反証を先に確認する。
